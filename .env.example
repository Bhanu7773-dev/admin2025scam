# Backend example env file for local development
# Copy to `.env` in development or set actual vars in your environment.

# CORS: comma-separated list of allowed origins
# Example: ALLOWED_ORIGINS=http://localhost:4000,http://localhost:3000
ALLOWED_ORIGINS=http://localhost:4000,http://localhost:3000

# (Recommended) Provide the Firebase service account JSON as a single-line string
# or point to a file path. See ENVIRONMENT.md for loading options.
# FIREBASE_SERVICE_ACCOUNT_JSON='{"type":...}'
# or
# FIREBASE_SERVICE_ACCOUNT_PATH=/absolute/path/to/serviceAccountKey.json
